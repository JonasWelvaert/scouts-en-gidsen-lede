extends layout

block content
  h1 #{groep.name}
  if groep.longname
    h4 #{groep.longname}
  p #{groep.description}
  img(src="/images/"+groep.name+".gif")   
  
  if (groep.name=="Leiding")
    Leiding
